<ul>
    <% moviesFromDB.forEach(function(movie) { %>
      <li>
        <a href="/movies/<%= movie._id %>"> <%= movie.title %> </a>
        <form action="/movies/<%= movie._id %>/delete" method="POST">
          <button type="submit">Delete</button>
        </form>
      </li>
    <% }); %>
  </ul>
  